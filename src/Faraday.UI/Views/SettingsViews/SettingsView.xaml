<UserControl x:Class="Faraday.UI.Views.SettingsViews.SettingsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:settingsViewModels="clr-namespace:Faraday.UI.ViewModels.SettingsViewModels"
             mc:Ignorable="d"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <StackPanel Grid.Column="0"
                    Orientation="Vertical">
            <!-- Title -->
            <TextBlock Text="Delete Accounts" HorizontalAlignment="Center" />
            <!-- Functionality -->
            <DataGrid ItemsSource="{Binding Accounts}" Margin="10" AutoGenerateColumns="False">
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Background" Value="Transparent" />
                        <Style.Triggers>
                            <DataTrigger Binding="{Binding IsActive}" Value="False">
                                <Setter Property="Background" Value="#A0E5E5E5" />
                                <Setter Property="FontStyle" Value="Italic" />
                            </DataTrigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>

                    <!-- Example data columns -->
                    <DataGridTextColumn Header="Name" Binding="{Binding Name}" />
                    <DataGridTextColumn Header="Balance" Binding="{Binding CurrentBalance}" />

                    <!-- Action buttons column -->
                    <DataGridTemplateColumn Header="Actions" Width="160">
                        <DataGridTemplateColumn.CellTemplate>
                            <DataTemplate>

                                <StackPanel Orientation="Horizontal" HorizontalAlignment="Left">
                                    <!-- Edit button -->
                                    <Button Content="Edit"
                                            Margin="0,0,6,0"
                                            Command="{Binding DataContext.EditAccountCommand,
                                          RelativeSource={RelativeSource AncestorType=DataGrid}}"
                                            CommandParameter="{Binding}" HorizontalAlignment="Left"/>

                                    <!-- Delete button -->
                                    <Button>
                                        <Button.Style>
                                            <Style TargetType="Button" BasedOn="{StaticResource {x:Type Button}}">
                                                <Setter Property="Content" Value="Deactivate" />
                                                <Setter Property="Command"
                                                        Value="{Binding DataContext.DeactivateAccountCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" />
                                                <Setter Property="CommandParameter" Value="{Binding}" />
                                                <Setter Property="HorizontalAlignment" Value="Left" />

                                                <Style.Triggers>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="False">
                                                        <Setter Property="Content" Value="Activate" />
                                                        <Setter Property="Command"
                                                                Value="{Binding DataContext.ActivateAccountCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" />
                                                    </DataTrigger>
                                                    <DataTrigger Binding="{Binding IsActive}" Value="True">
                                                        <Setter Property="Content" Value="Deactivate" />
                                                        <Setter Property="Command"
                                                                Value="{Binding DataContext.DeactivateAccountCommand, RelativeSource={RelativeSource AncestorType=DataGrid}}" />
                                                        <Setter Property="HorizontalAlignment" Value="Left" />
                                                    </DataTrigger>
                                                </Style.Triggers>
                                            </Style>
                                        </Button.Style>
                                    </Button>

                                </StackPanel>

                            </DataTemplate>
                        </DataGridTemplateColumn.CellTemplate>
                    </DataGridTemplateColumn>

                </DataGrid.Columns>
            </DataGrid>
        </StackPanel>
    </Grid>
</UserControl>