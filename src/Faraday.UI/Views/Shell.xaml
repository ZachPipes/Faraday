<Window x:Class="Faraday.UI.Views.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Faraday"
        xmlns:prism="http://prismlibrary.com/"
        mc:Ignorable="d"
        Title="Faraday" Height="450" Width="800">
    
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/> <!-- Navbar -->
            <RowDefinition Height="*"/> <!-- Body -->
        </Grid.RowDefinitions>
        
        <!-- Navbar -->
        <ContentControl Grid.Row="0" prism:RegionManager.RegionName="NavbarRegion" />
        
        <!-- Main -->
        <ContentControl Grid.Row="1" prism:RegionManager.RegionName="MainRegion" VerticalAlignment="Stretch" HorizontalAlignment="Stretch"/>
    </Grid>
</Window>
